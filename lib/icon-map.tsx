import { Code2, Palette, Megaphone, Briefcase, Server, type LucideIcon } from "lucide-react";

export const serviceIconMap: Record<string, LucideIcon> = {
  code: Code2,
  palette: Palette,
  megaphone: Megaphone,
  briefcase: Briefcase,
  server: Server,
};
